package test;

import java.util.stream.IntStream;

public class DummyTests {
    public static void main(String[] args) {
//        int[] a = {1, 2, 3, 4, 5};
//        int[] b = {2, 2, 2, 2, 2};


//        int sum = Arrays.stream(a).reduce(0, (subtotal, element) -> subtotal + element);
//        System.out.println(sum);

//        String[] names = {"Sam", "Pamela", "Dave", "Pascal", "Erik"};
//
//        List<String> nameList =
//                IntStream.range(0, names.length)
//                        .filter(i -> names[i].length() <= i)
//                        .mapToObj(i -> names[i])
//                        .collect(toList());
//
//        Integer sum = IntStream.range(0, a.length)
//                .mapToObj(i -> a[i]).reduce(0, (acc, elem) -> acc + elem);
//        System.out.println(sum);

        //String[] array = { "A", "B", "C", "D"  };

//        Streams.mapWithIndex(Arrays.stream(array),
//                        (str, index) -> index + " -> " + str)
//                .forEach(System.out::println);

//
//        String[] array = { "A", "B", "C", "D"  };
//
//        IntStream.range(0, array.length)
//                .mapToObj(index -> String.format("%d -> %s", index, array[index]))
//                .forEach(System.out::println);

//        int s = IntStream.range(0, a.length)
//                .mapToObj(i -> 2 * i )
//                .reduce(0, Integer::sum);
//        System.out.println(s);
    }
}
